// TODO: Create a function that states what to show based on answers from list
function switchRoles (role) {
    if (role === "Engineer") {


    } else if (role === "Intern") {

    } else {

    }
  };
  
 
  // TODO: Create a function to generate markdown for README
    
  module.exports = generateProfile;
  